
import Router from './routes/Router';

function App() {
  return (
    <div>
      {/*  here we call the Router and the router can display all other components */}
      <Router />
    </div>
  );
}
export default App;
